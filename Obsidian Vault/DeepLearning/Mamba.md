Mamba提出了一种选择性SSM机制,实现了线性复杂度,可用于超长序列处理
**创新:
* 将传统的SSM调整为将SSM参数作为输入函数,增加了对当前token的捕获
* 可以在序列边界处reset,使序列分离 
**总结:
* 为什么 Δ 先降到 1 维再广播**：若某步输入需**完全忽略**，应所有通道都忽略，故先 1D 再 Broadcast。
* 离散任务适合用实数SSM,连续任务适合用复数SSM